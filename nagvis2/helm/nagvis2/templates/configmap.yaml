apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nagvis2.fullname" . }}-env
  labels:
    {{- include "nagvis2.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
