apiVersion: v1
kind: Service
metadata:
  name: {{ include "nagvis2.fullname" . }}
  labels:
    {{- include "nagvis2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nagvis2.selectorLabels" . | nindent 4 }}
